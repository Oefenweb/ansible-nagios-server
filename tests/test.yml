# test file for nagios-server
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
  vars:
    nagios_server_private_keys:
      - src: "{{ playbook_dir }}/../files/id_rsa"
