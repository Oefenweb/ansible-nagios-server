{{ ansible_managed | comment }}
define host{
	use                     {{ item.host_template | default('generic-host') }}            ; Name of host template to use
	host_name               {{ item.name }}
	alias                   {{ item.alias }}
	address                 {{ item.address }}
}
